function seriesResistance(arr) {
    let Sum = 0;
    for(let i = 0 ; i < arr.length ; i++ ){
      Sum += arr[i];
    }
    if(Sum > 1){
      console.log(Sum);
    }
    else{
      console.log(Sum);
    }
  }
  seriesResistance([1,3,6,11]);